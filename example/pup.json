{
  "processes": [
    {
      "id": "dweb-example",
      "cmd": "deno run -A ../src/cli.ts start", 
      "autostart": true,
      "cluster": { "instances": 1 },
      "restart": "always",
      "restartDelayMs": 5000,
      "watch": ["dist", "dweb.config.ts"],
      "logger": {
        "stdout": "./logs/dweb.log",
        "stderr": "./logs/error.log"
      }	
    }
  ]
}

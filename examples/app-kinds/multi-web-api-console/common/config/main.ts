/** Shared defaults for web / api / console (each app may override). */
export default {
  language: "en-US",
  hotReload: true,
  logger: {
    level: "info",
    format: "text",
    output: { console: "auto" },
  },
};

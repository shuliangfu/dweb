/**
 * @fileoverview 供 `scheduledPlugin` 任务里 `script` 项调用的示例脚本。
 * 开发环境见 `src/config/main.dev.ts` 中 `plugins` 内的 `scheduledPlugin` 配置。
 */

console.log(
  `[view-hybrid-basic][scheduled-sample-script] ${new Date().toISOString()}`,
);
